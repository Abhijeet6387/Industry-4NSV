include navbar.pug
.bg-light.pt-5
  .bg-light
    .row
      #tb.container
        table.table
          thead(style='background-color: #cd5c5c; color: #f0f0f5;')
            tr
              th(scope='col') Specification
              th(scope='col') RequiredAt
              th(scope='col') PricePerUnit
              th(scope='col') NoOfUnits
              th(scope='col') Total
              th(scope='col') PurchasedQuantity
              th(scope='col') PriceInLakhs
              th(scope='col') BoughtFor     
              th(scope='col') Status            
          tbody
            if hardwareMESlist
              for item in hardwareMESlist
                tr
                  th(scope='row') #{item.Specification}
                  td #{item.RequiredAt}
                  td #{item.PricePerUnit}
                  td #{item.NoOfUnits}
                  td #{item.Total}
                  td #{item.PurchasedQuantity}
                  td #{item.PriceInLakhs}
                  td #{item.BoughtFor}
                  td #{item.Status}             
include footer.pug